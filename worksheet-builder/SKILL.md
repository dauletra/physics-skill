---
name: worksheet-builder
description: Помогает учителю составить, собрать или доработать печатный рабочий лист (worksheet) с заданиями для учеников — по физике или любому другому предмету. Используй этот skill, когда пользователь просит "сделай рабочий лист/карточку с задачами", "оформи эти задачи по физике", "нужен worksheet по кинематике/электричеству/оптике", "добавь график/таблицу в задание", "собери задачи в один документ для печати", "сделай вариант с ответами", а также когда просто присылают список готовых задач и просят их красиво и единообразно оформить для печати на принтере. Срабатывает и в разговоре по одному заданию за раз ("переделай второе задание", "убери график из третьего", "добавь ещё одно задание на соответствие") — не только когда явно просят "рабочий лист" целиком. Не зависит от раздела физики или даже от предмета — подходит для математики, химии и т.д., если нужен тот же принцип: единый визуальный стиль + шаблоны типовых заданий (открытый вопрос с/без места для решения, соответствие, верно/неверно, выбор варианта, классификация по категориям, упорядочивание, заполнить пропуск в тексте/таблице, дан график/построить график, задание из нескольких пунктов а)/б)/в) с общим графиком/условием).
---

# Worksheet Builder

Создаёт печатные рабочие листы (HTML, для печати на ч/б принтере через диалог
браузера) с гарантированно единым визуальным стилем: шрифт, поля, отступы,
нумерация, шапка — все листы, собранные этим skill'ом, выглядят как один
комплект, даже если готовились в разных сессиях.

## Модель работы: контент отдельно от вёрстки

Учитель редко формулирует задание идеально с первого раза — просит переделать
одно, добавить график в другое, убрать третье. Если бы весь лист был одним
HTML-документом, каждая такая правка требовала бы перечитать и переписать
целиком файл — дорого и рискованно для консистентности.

Поэтому рабочий лист собирается в два слоя:

1. **Черновик — JSON-файлы**, по одному на задание. Здесь и происходит вся
   работа с тобой в диалоге: обсуждение темы, формулировка задач, правки.
   Правишь только тот файл, который меняется — остальные не трогаешь и не
   перечитываешь.
2. **Сборка — детерминированный скрипт** (`scripts/render_worksheet.py`),
   который превращает JSON-черновики в готовый HTML. Скрипт, а не ты,
   отвечает за то, чтобы шрифты/отступы/нумерация были одинаковыми во всех
   заданиях — визуальную консистентность гарантирует код, а не память о
   том, что использовалось в прошлом сообщении.

**Никогда не пиши финальный HTML рабочего листа руками.** Твоя работа —
контент в JSON по схеме из `references/task-schema.md`; вёрстку делает скрипт.

## Фаза 1 — обсуждение и черновик

1. Проясни с учителем: тема, класс/возраст, цели урока, примерный набор
   типов заданий и сколько их. Не нужно требовать это всё
   сразу единым брифом — можно начинать с одного-двух заданий и добавлять по
   ходу разговора.
2. Заведи папку черновика в текущей директории учителя, слаг по теме
   (например `kinematics-9th-grade/`), и `meta.json` внутри — см.
   `references/task-schema.md`.
3. На каждое задание — свой файл `tasks/task-NN.json`. Задание — это дерево
   `blocks`, а не один тип на всё задание: блок — либо чистый компонент
   (`text`/`table`/`graph`/`list` — просто показывается, без буквы, без
   ответа), либо вопрос (один из 9 педагогических видов: `open`, `choice`,
   `match`, `fill_text`, `fill_table`, `plot`, `true_false`, `rank`,
   `classify` — несёт контент и ответ вместе, ответ инлайн, своей формой JSON
   для каждого вида), либо контейнер-подзадание `part` (см.
   `references/task-schema.md`, принципы — в
   `references/schema-design-principles.md`). Простое задание — компоненты
   условия + один вопрос, без `part`. Задание из нескольких пунктов а)/б)/в)
   — общий контекст (текст, график) на уровне задания, а затем по одному
   `part` на пункт: внутри — компоненты условия этого пункта + ровно один
   вопрос; буквы расставляются сами, баллы пункта — `points` на `part`.
   Голый вопрос рядом с `part` не ставится — если пунктов больше одного,
   каждый оборачивается в свой `part`. Если учитель прислал готовую задачу —
   просто оформи её в нужный вид вопроса. Если попросил "придумай 3 задачи
   по теме X" — сочиняй сам, ориентируясь на класс/уровень. Иллюстрации/
   схемы (наклонная плоскость, электрическая цепь и т.п.) сейчас не
   поддерживаются — только графики (`graph`/`plot`); если задача требует
   рисунка, опиши её условие текстом без изображения. Раскладка в несколько
   колонок (`layout`) пока не поддерживается — блоки идут друг под другом,
   график сам ограничен разумной шириной.
4. Греческие буквы, знаки сравнения, единицы измерения, простые степени/индексы
   и даже простые уравнения без дробей/корней — пиши обычным текстом (`α`,
   `≠`, `м/с²`, `υ₀`, `υ = υ₀ + at`), не LaTeX. Скорость обозначай греческой
   `υ` (ипсилон), не латинской `v` — см. предупреждение о похожих буквах в
   `references/symbols.md`. Там же — полный список готовых символов и когда
   индекс/степень не выразить Unicode-символом (нужны `<sup>`/`<sub>`) и когда
   выражение уже достаточно сложное, чтобы оправдать LaTeX (дробь, корень с
   содержимым, система уравнений — например, `$$E_k=\frac{m\upsilon^2}{2}$$`).
   Единицы измерения — СИ по
   умолчанию.
5. Для графиков (`graph`/`plot`) — не рисуй SVG сам, дай данные в
   `x_range`/`y_range`/`series` (см. `references/charts-and-graphs.md`).
6. Правка = редактирование одного `task-NN.json`. Не открывай и не переписывай
   другие файлы или уже собранный HTML ради одной правки.
7. Если учитель хочет посмотреть, как будет выглядеть конкретное задание
   прямо сейчас — необязательно собирать весь лист:
   ```
   python scripts/render_worksheet.py <папка> --task task-03
   ```
   выдаёт `task-03.preview.html` с этим заданием в реальных стилях.

## Фаза 2 — сборка

Когда учитель говорит "собери лист"/"готово, покажи целиком" — запусти:
```
python scripts/render_worksheet.py <папка>
```
Это создаст `<папка>/output/worksheet-student.html` (для учеников, без
ответов) и `<папка>/output/worksheet-teacher.html` (с ответами, выделенными
под каждым заданием). Оба файла — самодостаточный HTML, печатаются через
Ctrl+P в браузере (A4, поля и разбивка страниц уже настроены под ч/б печать).
На экране страницы уже видно, сколько листов получится при печати (см.
"Page-view" в `design-system.md`) — поля печати берутся из `print_margins` в
`meta.json` (`"default"`/`"none"`/`"minimum"`, те же варианты, что в диалоге
печати Chrome) и совпадают с тем, что применится при печати, если поля в
самом диалоге Chrome оставить как есть ("По умолчанию"). Чтобы поменять поля
— правь `print_margins` и пересобирай, в браузере это не переключить.

Если в диалоге печати Chrome включена галочка "Заголовки и нижние
колонтитулы" (включена по умолчанию), браузер сам допечатает мелкую строку
с датой/адресом страницы/номером страницы в поля — это настройка самого
Chrome, а не рабочего листа, и в HTML/CSS её не отключить; если учителю она
не нужна, надо снять галочку в диалоге печати ("Ещё настройки").

Скажи учителю, что оба файла готовы и где они лежат; предложи открыть в
браузере, чтобы проверить перед печатью. В браузере ничего в вёрстке
поправить нельзя (никаких тулбаров/hover-кнопок) — любая правка (место для
решения, раскладка вариантов ответа, раскладка компонентов) идёт через
соответствующее поле JSON и пересборку.

## После сборки — правки

Если после просмотра собранного листа нужно что-то поменять: правь
соответствующий `task-NN.json` (или `meta.json`, если это шапка/порядок) и
запусти сборку заново. Пересборка — мгновенная и бесплатная (детерминированный
скрипт, не LLM-генерация), можно повторять сколько угодно раз, не тратя
контекст на чтение HTML.

## Справочники

- `references/task-schema.md` — полная схема `meta.json`, дерева блоков
  (`part`, 4 чистых компонента `text`/`table`/`graph`/`list`) и всех 9 видов
  вопросов. Читай перед созданием/правкой JSON, особенно для видов, которые
  давно не использовал.
- `references/schema-design-principles.md` — принципы, по которым устроена
  схема. Нужен, только если проектируешь новый вид блока (компонент/вопрос),
  а не при повседневном составлении заданий.
- `references/symbols.md` — готовые Unicode-символы (операторы, сравнение,
  греческий алфавит, над/подстрочные цифры) и правило, когда переходить на
  `<sup>`/`<sub>` или на LaTeX. Проверяй здесь перед тем, как писать `$...$`
  ради одной буквы или значка.
- `references/design-system.md` — визуальная система (шрифты, поля, нумерация,
  шапка). Обычно не нужен для повседневной работы — редактируется только если
  учитель просит поменять стиль для *всех будущих* листов, а не для одного
  задания.
- `references/charts-and-graphs.md` — формат данных графика (`x_range`/
  `y_range`/`series`) и ч/б-безопасный визуальный код рендерера.
- `scripts/render_worksheet.py` — CLI-точка входа рендерера (`--help` покажет
  все опции); сама логика разнесена по `scripts/components/` (чистые
  компоненты), `scripts/questions/` (9 педагогических видов), `task.py`,
  `render_helpers.py`, `visuals.py`, `document.py`, `assets.py`,
  `strings.py`.
- `examples/kinematics-9th-grade/` — рабочий пример черновика, покрывающий
  все 9 видов вопросов и оба чистых визуальных компонента (`text`/`graph`) —
  включая задание с двумя подзаданиями-`part` а)/б) вокруг одного общего
  графика (`task-10`), задание с двумя `part`, каждый с своим `plot`, для
  построения графика p-V/p-T по данному V-T (`task-13`), и задание 9,
  которое демонстрирует иерархию текст/`<sup>`-`<sub>`/LaTeX из `symbols.md`
  на практике. Запусти на нём рендерер, если нужно быстро посмотреть, как
  выглядит готовый лист, или использовать как образец при написании нового
  `task-NN.json`.
